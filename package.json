{
  "name": "cook-cart-ai",
  "version": "1.0.0",
  "description": "Full-stack AI grocery planner with cart, checkout, and Stripe sandbox payments.",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "npm --prefix server run dev",
    "client": "npm --prefix client run dev"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
